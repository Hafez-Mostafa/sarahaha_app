<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>

    
    <link rel="stylesheet" href="/css/style.css">
    <title>Sarahah App</title>
</head>

<body>

    <header class="bg-gray-800 text-white py-4">
        <div class="container mx-auto flex justify-between items-center px-4">
            <!-- Logo -->
            <div class="text-2xl font-bold">
                <a href="/" class="text-white hover:text-gray-400"> <svg
      fill="none"
      stroke-width="1.5"
      stroke="currentColor"
      viewBox="0 0 24 24"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
      class="w-12 h-10"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M3 8l9 6 9-6m-9 6v6m-9-6v6m18-6v6m-9-6l-9 6m18-6l-9 6"
      />
    </svg>

</a>
            </div>

            <!-- Navigation Links -->
            <nav class="space-x-6 hidden md:flex">
                <a href="/" class="text-gray-300 hover:text-white">Home</a>
                <a href="#" class="text-gray-300 hover:text-white">About</a>
                <a href="#" class="text-gray-300 hover:text-white">Services</a>
                <a href="#" class="text-gray-300 hover:text-white">Contact</a>
            </nav>

            <!-- Buttons -->
            <div class="space-x-4">
                <% if (session.loggedIn) { %>
                    <a href="/profile" 
                        class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-4 rounded">Profile</a>

                    <a href="/logout"
                        class="bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded">Logout</a>
                        
                <% } else { %>
                    <a href="/signin"
                        class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded">Signin</a>
                    <a href="/register"
                        class="bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-4 rounded">Register</a>
                <% } %>
                
            </div>
        </div>
    </header>